name: Build TALLY

# 出包：macOS dmg + Windows exe（NSIS 安装器 + 便携 exe）
# 触发：手动运行（workflow_dispatch）或推送 v* 标签
# 注意：本仓库根即 tally-tauri 项目（package.json 在根下）
on:
  workflow_dispatch:
  push:
    tags: ['v*']

jobs:
  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json
      - uses: dtolnay/rust-toolchain@stable
      - uses: swatinem/rust-cache@v2
        with:
          workspaces: src-tauri
      - name: Install frontend deps
        run: npm ci
      - name: Build dmg
        run: npx tauri build --target aarch64-apple-darwin
      - name: Upload dmg
        uses: actions/upload-artifact@v4
        with:
          name: TALLY-macOS-aarch64
          path: src-tauri/target/aarch64-apple-darwin/release/bundle/dmg/*.dmg

  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json
      - uses: dtolnay/rust-toolchain@stable
      - uses: swatinem/rust-cache@v2
        with:
          workspaces: src-tauri
      - name: Install frontend deps
        run: npm ci
      - name: Build Windows installers
        shell: bash
        run: npx tauri build --bundles nsis
      - name: Upload exe artifacts
        uses: actions/upload-artifact@v4
        with:
          name: TALLY-Windows-x64
          path: |
            src-tauri/target/release/bundle/nsis/*.exe
            src-tauri/target/release/tally-tauri.exe

  release:
    # 打标签时自动创建 GitHub Release 并附上全部产物
    needs: [build-macos, build-windows]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          merge-multiple: true
          path: artifacts
      - uses: softprops/action-gh-release@v2
        with:
          # 顶层（dmg / 便携 exe）+ NSIS 安装器（嵌套在 bundle/nsis/ 下）
          files: |
            artifacts/*
            artifacts/bundle/nsis/*.exe
